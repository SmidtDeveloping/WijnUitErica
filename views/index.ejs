<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WijnUitErica</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/styling.css" class="css">
</head>

<body>
    <%- include("partials/nav") %>

        <section id="hero" class="hero d-flex flex-column align-items-center justify-content-center text-center">
            <h1 class="display-3 text-black mb-3 fw-bold">Wijn Uit Erica</h1>
            <p class="lead text-black mb-5 fw-light">Eigen wijn gemaakt in Erica</p>
            <a href="/producten" class="btn btn-lg btn-primary">Bekijk onze wijnen</a>
        </section>

        <section class="py-5">
            <div class="container">
                <div class="row align-items-center">

                    <div class="col-md-6 text-center text-md-start mb-4 mb-md-0">
                        <h2 class="fw-bold mb-4">Over Ons</h2>
                        <p class="lead">Wij maken ambachtelijke wijnen in Erica met passie en traditie. Proef het
                            verschil!</p>
                    </div>

                    <div class="col-md-6">
                        <h2 class="fw-bold text-center mb-4">Populaire Producten</h2>
                        <div class="row g-4">

                            <% popular.forEach(product=> { %>
                                <div class="col-6 col-md-6 col-lg-6">
                                    <div class="card h-100 d-flex flex-column">
                                        <img src="<%= product.img %>" class="card-img-top img-fluid"
                                            alt="<%= product.naam %>" style="height: 200px; object-fit: cover;" />

                                        <div class="card-body">
                                            <h5 class="card-title">
                                                <%= product.naam %>
                                            </h5>
                                            <p class="card-text">
                                                <%= product.description %>
                                            </p>
                                            <div class="position-absolute d-flex gap-1" style="top: -12px; right: 0;">
                                                <% product.cat.forEach(cat=> { %>
                                                    <span class="badge rounded-pill bg-primary">
                                                        <%= cat.name %>
                                                    </span>
                                                    <% }) %>
                                                        <span class="badge rounded-pill bg-danger">POPULAIR</span>
                                                        <% if (product.nieuw) { %>
                                                            <span class="badge rounded-pill bg-danger">NIEUW</span>
                                                            <% } %>
                                            </div>
                                            <% product.smaakprofiel.forEach(smaak=> { %>
                                                <span class="badge text-bg-primary">
                                                    <%= smaak %>
                                                </span>
                                                <% }) %>
                                                    <br><br>
                                                    <a href="/product/<%= product.id %>"
                                                        class="btn btn-primary mt-2">Bekijk</a>
                                        </div>
                                    </div>
                                </div>
                                <% }) %>

                        </div>
                    </div>

                </div>
            </div>
        </section>



        <%- include("partials/footer") %>





</body>

</html>